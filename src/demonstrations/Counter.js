/* eslint-disable prettier/prettier */

import React from "react";
import { useState, useEffect } from "react";

//useEffect let us perform side effects within our funstion components
//we can pass in a dependecny array which lets you specify what data , when changed , you would like to trigger a re-render
// we can additionly use a cleanup function to stop any of our side effects before the component unmounts

